# 品牌规则

## 命名规则

- 战略文件使用固定文件名
- 运营文件按月份或日期命名
- 草稿建议使用 `日期_平台_主题_状态` 结构

## 内容边界

- 不泛讲所有 AI 话题
- 不做新闻搬运号
- 不把重心放在单个工具介绍

## 复用规则

能长期复用的内容，优先写进 `01` 到 `04` 和 `08`。

只属于当前账号、当前时间窗口的内容，优先放进 `06_运营中心`。
