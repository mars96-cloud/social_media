# Storyboard

## 01 一上来就写

土豆把一句模糊指令直接塞进一个大写稿口，另一边吐出一团散乱稿纸。红字提醒：`像样`、`不对味`。

## 02 写给谁

土豆站在三岔路口前，三块小牌子指向：`新手`、`创作者`、`经营者`。橙色路径只会选一条。

## 03 判断是什么

土豆把同一个主题球塞进左右两个不同模具，出来两条不同路线。蓝字补充：`主题不是判断`。

## 04 先排结构

土豆先把四块板子摆成一条线：`对象` -> `判断` -> `结构` -> `AI填`。最后一格由 AI 小机械臂负责搬运纸张。
